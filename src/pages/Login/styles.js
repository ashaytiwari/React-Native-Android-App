import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  loginView: {
    padding: 30
  },
  signinTxt: {
    color: '#2e3192',
    fontSize: 40
  }
});

export default styles;