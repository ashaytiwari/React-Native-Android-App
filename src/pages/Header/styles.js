import React from 'react';
import {
  StyleSheet
} from 'react-native';

const styles = StyleSheet.create({
  header: {
    backgroundColor: '#05668d',
    marginBottom: 0,
  },
  headerText: {
    color: '#ffffff',
    fontSize: 20
  }
})

export default styles;